server:
 port: ${server-port:8763}
eureka:
 instance:
  hostname: ${springboot.app.name:eureka}
 client:
  registerWithEureka: false
  fetchRegistry: false
  serviceUrl:
   defaultZone: http://${registry.host:localhost}:${server.port}/eureka/

